import { Suspense } from "react";
import { CreatorWorkspace } from "@/components/creator/CreatorWorkspace";

export default function CreatePage() {
  return <Suspense><CreatorWorkspace /></Suspense>;
}
